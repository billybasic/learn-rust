name: Rust

on:
  push:
    branches: [ "main", "tutorial/web_app" ]
  pull_request:
    branches: [ "main" ]

env:
  CARGO_TERM_COLOR: always

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    - name: Build
      run: |
        cd web_app
        cargo build --verbose
    - name: Run tests
      run: |
        cd web_app
        cargo test --verbose
